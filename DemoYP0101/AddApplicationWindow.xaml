<Window x:Class="DemoYP0101.AddApplicationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DemoYP0101"
        mc:Ignorable="d"
        Title="AddApplicationWindow" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Адрес -->
        <TextBlock Text="Адрес:" Grid.Row="0" Grid.Column="0" 
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox x:Name="AddressComboBox" Grid.Row="0" Grid.Column="1" 
                  Margin="0,0,0,10" Height="35"/>

        <!-- Заявитель -->
        <TextBlock Text="Заявитель:" Grid.Row="1" Grid.Column="0" 
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox x:Name="UserComboBox" Grid.Row="1" Grid.Column="1" 
                  Margin="0,0,0,10" Height="35"/>

        <!-- Телефон -->
        <TextBlock Text="Телефон:" Grid.Row="2" Grid.Column="0" 
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox x:Name="PhoneTextBox" Grid.Row="2" Grid.Column="1" 
                 Margin="0,0,0,10" Height="25"/>



        <TextBlock Text="Статус:" Grid.Row="4" Grid.Column="0" 
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox x:Name="StatusComboBox" Grid.Row="4" Grid.Column="1" 
                  Margin="0,0,0,10" Height="35"/>

        <!-- Описание проблемы -->
        <TextBlock Text="Описание проблемы:" Grid.Row="5" Grid.Column="0" 
                   Margin="0,0,10,10" VerticalAlignment="Top"/>
        <TextBox x:Name="DescriptionTextBox" Grid.Row="5" Grid.Column="1" 
                 Margin="0,0,0,92" Height="100"
                 TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"/>


        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Center" 
                    Margin="0,0,0,10">
            <Button x:Name="SaveButton" Content="Сохранить" 
                    Width="178" Height="30" 
                    Margin="0,0,20,0" Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" Content="Отмена" 
                    Width="100" Height="30" 
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>